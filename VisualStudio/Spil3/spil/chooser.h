#pragma once
#include <iostream>
#include <string>
#include <time.h>
#include <Windows.h>

using namespace std;

void jobChooser(string* job, int* loen);
void setBankAccount(double* saldo, int* pin);
void setTime(int*, int*, int*);
void setCar(string*, int*, int*);